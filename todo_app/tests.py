"""
tests.py
Tento soubor obsahuje testy pro aplikaci.
Jeho účelem je testovat funkčnost jednotlivých částí aplikace, jako jsou modely, views nebo formuláře.
Testy nám pomáhají ověřit, že aplikace funguje podle očekávání a nedochází k regresím (nový kód nerozbije starý) při změnách v kódu.
"""

from django.test import TestCase

# Create your tests here.
